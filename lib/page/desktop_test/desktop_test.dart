import 'package:flutter/material.dart';

class DesktopTestPage extends StatelessWidget {
  const DesktopTestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
